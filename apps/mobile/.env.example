# LoopOS v0.1 — local-only mode
# No env vars required for local APK validation.
# The app stores all data on-device using AsyncStorage.

# ─── Future Supabase mode (not active yet) ───────────────────────────────────
# EXPO_PUBLIC_SUPABASE_URL=
# EXPO_PUBLIC_SUPABASE_ANON_KEY=
# EXPO_PUBLIC_USER_ID=user_test_1

# ─── Legacy API Express mode (not active) ────────────────────────────────────
# EXPO_PUBLIC_API_URL=http://localhost:3333
