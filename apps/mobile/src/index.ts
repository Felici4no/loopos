/**
 * LoopOS Mobile — Entry Point
 *
 * Screens, navigation, and state management will be implemented in Etapa 2+.
 * This file bootstraps the Expo app.
 */

import { registerRootComponent } from 'expo';
import { App } from './App';

registerRootComponent(App);
