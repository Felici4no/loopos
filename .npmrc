# React Native/Expo precisa de node_modules "achatado" para o Gradle
# resolver pacotes transitivos (@react-native/gradle-plugin etc.).
# Recomendação oficial do Expo para monorepos pnpm:
# https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
