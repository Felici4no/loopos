# LoopOS — Variáveis de ambiente SERVER-SIDE (Edge Functions / @supabase/server)
#
# Estas variáveis NUNCA vão para o app mobile (Expo/React Native).
# Use apenas em ambientes server-side reais: Supabase Edge Functions,
# um futuro backend Express, ou scripts administrativos locais.
#
# Ver docs/supabase-server-sdk.md para o que cada uma faz e por que
# SUPABASE_SECRET_KEY nunca pode ser exposta no cliente.

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
SUPABASE_JWKS_URL=
